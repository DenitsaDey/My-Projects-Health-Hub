@model IEnumerable<DoctorsViewModel>

<section id="doctors" class="doctors">
    <div class="container">
        <div class="row">
            @if (!Model.Any())
            {
                <h2 class="text-center"> No doctors found by the given criteria!</h2>
            }
            @foreach (var doctor in Model)
            {
                <div class="col-lg-6">
                    <div class="member d-flex align-items-start">
                        <div class="pic">
                            <img src="@doctor.ImageUrl" class="img-fluid" alt="">
                                         <a asp-area="" asp-controller="Doctors" asp-action="Details" asp-route-doctorId="@doctor.Id"></a>
                        </div>
                        <div class="member-info">
                            <h4>@doctor.FullName</h4>
                            <span>@doctor.SpecialtyName</span>
                            <p>@doctor.ClinicName</p>
                            <div class="form-group" style="padding-top:30px">
                                <a class="appointment-btn scrollto" asp-area="" asp-controller="Doctors" asp-action="Details"
                                   asp-route-doctorId="@doctor.Id">
                                        <h7>Book an Appointment</h7>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</section>
